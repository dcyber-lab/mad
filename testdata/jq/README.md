These files are copied unchanged from jq 1.7.1 (https://github.com/jqlang/jq,
tag `jq-1.7.1`, directory `tests/`) and are used under jq's MIT license,
reproduced in `COPYING`. `skip.txt` is ours: it lists the cases jqgo
deliberately handles differently, with the reason for each.
